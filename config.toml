[solution]
name = "gdn-cute-decode-v1"            # Solution name
definition = "gdn_decode_qk4_v8_d128_k_last"   # Benchmark definition name
author = "team"                        # Team/author name

[build]
language = "python"                    # python | triton | cuda
entry_point = "msinfer_entry.py::run"  # <file>::<symbol> — run_prefill for prefill track
dependencies = ["nvidia-cutlass-dsl", "cuda-python>=12.8"]
destination_passing_style = true
